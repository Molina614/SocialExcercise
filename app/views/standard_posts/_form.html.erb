<%= form_with(model: standard_post, local: true) do |form| %>
  <% if standard_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(standard_post.errors.count, "error") %> prohibited this standard_post from being saved:</h2>

      <ul>
      <% standard_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="ui form">
    <div class="form-group">
        <%= form.label :content %>
        <%= form.text_area :content %>
    </div>
   </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
